# あいのて local Web を別のPCで試す

これは各社PCで`serve.py`を起動し、同じPCのブラウザから`127.0.0.1`を使う手順です。
あいのてはSaaSでもnative appでもありません。正本は検査済みsource ZIPとこの起動手順です。
基本機能の確認にインターネット接続は要りません。外部AI・PRS・LINE等は会社が明示設定し、
画面で操作した場合だけ別契約の接続先を使います。

検証済み環境は **macOS 26.4および26.6.2 / Apple Silicon / Python 3.14** です。local Web sourceはnative appでは
ありませんが、Windowsはclean-room未検証のため初期版のサポート対象外です。

## 1. 用意するもの

基本画面の確認には **Python 3.12 以上**が必要です。暗号化バックアップとOffice出力には、
下記の固定依存も必要です。installは人間の管理作業として、配布先PCごとに明示して行います。

暗号化バックアップを使う場合は、標準AEAD実装を入れます:

```
python3 -m pip install -r requirements.txt
```

未導入時は弱い暗号方式へ切り替えず、バックアップ導線を停止理由付きで隠します。

書類を Word / Excel の形で書き出したいときは、2つの書類エンジンとその推移依存が要ります
（無くても画面と印刷用PDFは使えます）。検証済みの全バージョンを次のファイルで固定しています:

```
python3 -m pip install -r requirements-office.txt
```

- `python-docx` … 重要事項説明書を Word（国交省様式）で出す
- `openpyxl` … Excel の名簿を取り込む・A/C/BのExcel様式で出す
- `Pillow` … 物件写真と間取り図をExcel内部へ埋め込む

入れていない場合、その出力ボタンは表示しません。他の機能は影響を受けません。

PDFは「印刷用に開く（PDF保存）」を使い、ブラウザの印刷画面から保存します。
通常起動と通常回帰は`RI_HUB_DISABLE_SOFFICE=1`でLibreOffice探索・起動を止めます。
LibreOffice実変換はProblem Report対策として、明示的に許可された単発・非並列の手順でのみ行います。

入っているか調べる（ターミナルで）:

```
python3 --version
```

`3.12` 以上と出れば大丈夫です。出ない・古い場合は https://www.python.org/downloads/ から入れてください。

> なぜ 3.12 以上か: 画面を組み立てる部分に、3.12 から入った書き方を使っているためです。
> 3.11 以下では起動できません（起動時にその旨を表示します）。

## 2. 起動する

配布元のパソコンで、まず検査済みのlocal Web source ZIPを作ります:

```
python3 build_demo_bundle.py
```

このコマンドは、配布用として確定した製品利用条件 `LICENSE` が同じフォルダに
存在するときだけ配布ZIPを作ります。ライセンス未選択の状態では配布できません。

作られた `dist/ainote-local-web-source.zip` **だけ**を別のパソコンへ渡して展開してください。
開発フォルダを丸ごと渡すと、作業データや配布対象外の原紙まで混ざるため使いません。
展開した `ainote-local-web` フォルダの中で:

```
RI_HUB_DISABLE_SOFFICE=1 python3 start_ainote.py
```

ブラウザが自動で開きます（開かないときは **http://127.0.0.1:8788/**）。止めるときは Ctrl-C です。

よく使う指定:

```
python3 start_ainote.py --onboard        # 「はじめての設定」から試す
python3 start_ainote.py --demo-data      # 見本データを明示的に入れて試す
python3 start_ainote.py --port 8790      # ポートを変える
python3 start_ainote.py --no-browser     # ブラウザを開かない
```

- 通常の初回は空の保存先から法人登録へ進みます。見本は`--demo-data`を付けた場合だけ用意します。
- データの保存先は `~/ainote-data/out`。
- `./start_ainote.sh` でも起動できます（Python を自動で探します）。

## 3. デモで見せる順番（そのまま通ります）

1. **はじめての設定** — 会社のこと → ログインを作る → AIをどうするか、の3画面。
   1画面で聞くことを1つに絞っています。パスワードはURLに載りません。
2. **ホーム** — 「きょうは何をしますか」。やりたいことの札が6枚。
3. **マイソクを作る** — 4画面。1画面3〜6項目だけ。前の答えは持ち回ります。
4. **重要事項説明書を作る** — 5画面。会社名・免許番号・宅建士は業者情報から自動で入るので聞きません。
5. **物件** — 物件ごとに「マイソクを作る／重要事項説明書を作る／お金を計算する」が並びます。
6. **お客様の問い合わせ** — 各件から「お返事の下書きを作る」へ。
7. **お金を計算する** — 自分の金額を入れられます。
8. **業者情報 → 変更の履歴** — 直したあと、いつでも前の状態に戻せます。戻しても履歴は消えません。
9. **接続設定** — FAX送信・メール・AI・PRS重説調査の繋ぎ口。PRSは`juusetsu-hazard-v1`の接続状態と最終receiptを表示し、**繋いでも勝手には送りません**。
10. **暗号化バックアップ** — データ本体をAES-256-GCMで暗号化して保存し、復旧キーは別ファイル・別の場所に保管します（合わなければ復元して元のやり方に戻せます）。

### バックアップを別の場所へ復元する

あいのてを終了してから、暗号化バックアップ、別保管した復旧キー、空の復元先を明示して実行します。
既存の `out`、`auth`、監査鍵には上書きできません。世代が混ざる事故を防ぐため、必ず新しい名前を使います。

```bash
python3 -m hub_core.backup portable-restore ~/Downloads/ainote-backup.enc \
  --dest-data-dir ~/ainote-restored/out \
  --recovery-key ~/Documents/ainote-recovery-key.txt \
  --audit-key-dest ~/.ri-hub/restored-keys/audit_chain.key
```

復元内容を別ポートで確認します。確認中は、復元した監査鍵を明示します。

```bash
RI_HUB_AUDIT_KEY_PATH=~/.ri-hub/restored-keys/audit_chain.key \
  python3 serve.py --data-dir ~/ainote-restored/out --port 8790
```

復旧キーはバックアップ本体・復元した監査鍵と同じ場所へ置かないでください。

## 4. 通し確認を自分で回す

デモの前に、その端末で全部通るかを機械で確かめられます:

```
RI_HUB_DISABLE_SOFFICE=1 python3 demo_walkthrough.py
```

まっさらな状態から48項目（設定 → 法人/物件 → 顧客取込/接続 → LINE read-only接続テスト →
マイソク → Excel/ブラウザ印刷PDF → 重説 → 監査/履歴/復元）を実際のHTTPで叩いて確かめます。
全部通れば `48 / 48 通過` と出ます。転んだ箇所は理由付きで出ます。

開発元のリポジトリで全機能を確認するテストはこちらです。配布用ZIPにはテストコードを
含めていないため、展開先では上の `demo_walkthrough.py` を使います:

```
RI_HUB_DISABLE_SOFFICE=1 python3 -m unittest discover -s . -p "test_*.py"
```

> ⚠️ このテストは、このフォルダの中の `out/` を書き換えます（お試し用のデータを作り直すため）。
> `out/` に自分のデータを置いている場合は、先に別の場所へコピーしてください。
> 本番のデータ（`~/ainote-data/out`）には触りません。

## 5. デモで気をつけること

- **お客様に渡すもの（マイソク・重要事項説明書・LINE・お客様ページ）は、すべて利用会社の名前で出ます。**
  「あいのて」の名前や色は出ません。ここは製品の設計の芯なので、デモでも触れる価値があります。
- 会社名・免許番号などが未登録の成果物には**下書き**と明記し、配布できる完成稿としては扱いません。
  別会社の名前で埋めることはしません（宅建業法の必要表示事項のため）。
- AIは既定で「使わない」です。選ばない限り、外部へは何も送りません。
- FAXは繋ぐ仕組みまでできています。**実際に送る確認はまだ取れていません**（サービスの契約が要るため）。
  デモでそこまで見せる場合は、この点を正直に伝えてください。

## 6. うまくいかないとき

| 症状 | 見るところ |
|---|---|
| 起動しない | `python3 --version` が 3.12 以上か。古ければ入れ直す |
| 初回設定へ進まない | 起動時に表示された保存先と`http://127.0.0.1:<port>/setup`を確認する |
| ポートが使用中 | `python3 start_ainote.py --port 8790` のように変える |
| PDFの保存 | 「印刷用に開く（PDF保存）」からブラウザで保存する。通常運用でLibreOffice直接変換は使わない |
| 元に戻したい | あいのてを終了し、保存先フォルダ（`~/ainote-data`）を別名へ移してから再起動します。復元不要と確認できるまで削除しません |

## 7. LINE を使いたい場合（デモには不要）

**デモにはインターネットもCloudflareも要りません。** ここまでの機能は全部この端末の中で完結します。

ただし LINE の機能（物件カードの送信・内見予約・お客様からの受信）を**実際に動かす**には、
LINEから見える公開のHTTPSの置き場が要ります。LINE側からこちらへ通知が届く仕組みのため、
自宅や事務所のパソコンだけでは成立しません。

必要になるもの:

| 何のため | 要るもの | 設定 |
|---|---|---|
| LINEの送受信の中継 | line-harness（Cloudflare Workers で動かす想定） | `LINE_HARNESS_API_URL` / `LINE_HARNESS_API_KEY` / `LINE_HARNESS_ACCOUNT_ID` |
| 内見予約の画面（LIFF） | LINE Developers で作る LIFF | `AINOTE_LIFF_ID` |
| 物件写真の置き場 | 公開サイト（Cloudflare Pages で動かす想定） | `LIFF_PAGES_URL` |

**未設定のときは何も送りません**（モックのまま）。予約リンクも写真も出しません。
開発元のURLを既定値にはしていないので、設定しないまま別の会社のお客様が
知らない場所へ飛ぶことはありません。

CloudflareとLINEの料金・上限・規約は利用開始時の公式情報で確認してください。
LINE公式アカウントの開設と中継の配置は、その会社ごとに行う作業です（デモの範囲外）。
